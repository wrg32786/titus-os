#!/bin/bash
# Hook: Stop — fires when Claude Code session ends or stops
# Reminds Titus to run /close if session had meaningful work

echo "[SESSION] Session ending. If this was a working session, run /close to commit memory updates."